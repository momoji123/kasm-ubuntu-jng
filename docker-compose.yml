version: '3.8'

services:
  build-img:
    build:
        context: .
        dockerfile: dockerfile
        args:
            CUST_USER: <username>
            CUST_USER_PW: <cust_user_password>
    image: registry.mokican.com/kasm-ubuntu-jng:dev-V0.1